scapy==2.5.0        # For packet sniffing and manipulation
pytest==7.2.2        # For running unit tests
requests==2.28.1     # For any potential HTTP requests or external API interactions
flask==2.2.3         # If you plan to expose the firewall's functionality via a web interface
python-iptables==0.14.0 # For interacting with the iptables firewall system (if used)
termcolor==2.2.0     # For colored terminal output (helpful for logging and CLI tools)
tabulate==0.9.0      # For pretty printing tabular data (useful for displaying rules)
psutil
colorama
tqdm
PyQt5==5.15.9  # Required for the GUI (kavachgui)
NetfilterQueue==0.8.1  # For interacting with iptables in user space
pyqtgraph==0.13.3  # For GUI visualizations and live traffic/statistics graphs
